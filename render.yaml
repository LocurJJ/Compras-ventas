services:
  - type: web
    name: compras-ventas
    runtime: python
    buildCommand: ""
    startCommand: "HOST=0.0.0.0 python server.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
